<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://fonts.googleapis.com/css2?family=Creepster&family=Finger+Paint&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap" rel="stylesheet">

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GPK DB: <%=card.name%> Info</title>
</head>
<style>
    body {
        background-image: url('assets/Untitled_Artwork 4.jpg');
        font-family: 'Creepster', cursive;
        color: red;
        text-align: center;
        
    }
    h1 {
        font-family: 'Creepster', cursive;
        font-size: 3em;
        color: red;
    }
    a {
        text-decoration: none;
        color: #333;
        margin: 0 10px;
    }

    button {
        font-family: 'Creepster';
    }
</style>
<body>
    <h1>
        <%=card.name%>
    </h1>
    <p>card number:
        <%=card.number%></p>
    <p>series:
        <%=card.series%></p>
    <p>condition:
        <%=card.condition%></p>
        
    <% if(card.isAuth){ %>
        <p>This card is authenticated by a verified source.</p>
    <% } else { %>
        <p>This card is <b>not</b> authenticated by a verified source.</p>
    <% } %>
    
    <a href="/cards">Index</a><br>
    <a href="/cards/<%=card._id%>/edit">Edit</a><br>
    <a href="/series<%=card.series%>">View Cards added from series <%=card.series%></a>
    <form action="/cards/<%=card._id%>?_method=DELETE" method="POST">
        <button type="submit">Delete card <%=card.number%>, <%=card.name%></button>

</body>
</html>